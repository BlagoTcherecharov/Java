let word = "next";
let result = [];

function difficulty_selection()
{
    let value = document.querySelector('#difficulty');
    let difficulty = value.value;
    alert(difficulty);
}

function init()
{
    var answer = "";
    var life = 6;
    
    for(i = 0; i < word.length; i++)
    {
        result[i] = "_ ";
    }

    document.getElementById("word_length").innerHTML = result.join(" ");
    document.getElementById("wrong_letters").innerHTML = "Wrong letters: ";
    document.getElementById("lives").innerHTML = "You have " + life + " lives!";
}

window.onload = init();

/*
function word_length()
{
    result = word.replace(word, "_ ");
    document.getElementById("word_length").innerHTML = result;
}
*/


function letter_submit()
{
    let letter = document.getElementById("letter").value;
    
    for(var i = 0; i < word.length; i++)
    {
        if(letter === word[i])
        {
            result[i] = letter;
            document.getElementById("word_length").innerHTML = result.join(" ");
        }
    }
} letter_submit();

